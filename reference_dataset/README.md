# Reference dataset (local seed input)

Place styled room images here for `python database/seed/run_seed.py`.

Folder layout is defined in `database/seed/config.py` (`STYLE_FOLDERS`). Images are not committed to git; only this README is tracked.
